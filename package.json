{
  "name": "treasure-key-leaderboard",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --product hosted-service recoveringcultist/treasure-key-leaderboard",
    "create-local": "graph create --node http://localhost:8020/ recoveringcultist/treasure-key-leaderboard",
    "remove-local": "graph remove --node http://localhost:8020/ recoveringcultist/treasure-key-leaderboard",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 recoveringcultist/treasure-key-leaderboard"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.21.1",
    "@graphprotocol/graph-ts": "0.20.0"
  }
}
